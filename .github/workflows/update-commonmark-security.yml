name: Update CommonMark Security

on:
  push:
    branches:
      - feat/config-guard-smoke-test

permissions:
  contents: write

jobs:
  update-commonmark:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/config-guard-smoke-test

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: none

      - name: Update CommonMark to a patched release
        run: composer update league/commonmark --with-all-dependencies --no-interaction --prefer-dist

      - name: Commit lock file
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add composer.lock
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "chore: update CommonMark security fixes"
          git push origin HEAD:feat/config-guard-smoke-test
